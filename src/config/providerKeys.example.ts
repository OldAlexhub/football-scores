/**
 * Copy this file to providerKeys.ts (already git-ignored) and fill in your
 * own free-tier API tokens. Football Scores Today never ships without this
 * file — if it is missing, providerKeys.ts falls back to empty strings and
 * the app automatically relies on OpenFootball + CachedProvider only.
 *
 * football-data.org: https://www.football-data.org/client/register
 * API-Football (api-sports.io): https://dashboard.api-football.com/register
 */
export const FOOTBALL_DATA_ORG_TOKEN = '';
export const API_FOOTBALL_KEY = '';
